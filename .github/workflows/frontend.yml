name: Frontend contract

on:
  push:
    paths:
      - "app/**"
      - "components/**"
      - "hooks/**"
      - "lib/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "app/**"
      - "components/**"
      - "hooks/**"
      - "lib/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/frontend.yml"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout frontend
        uses: actions/checkout@v4

      - name: Checkout latest backend contract
        continue-on-error: true
        id: checkout_branch
        uses: actions/checkout@v4
        with:
          repository: SE-FCaps-Tool/f-caps-schedule-be
          ref: ${{ github.head_ref || github.ref_name }}
          path: .contract-be
          fetch-depth: 1

      - name: Checkout backend contract (fallback to main)
        if: steps.checkout_branch.outcome == 'failure'
        uses: actions/checkout@v4
        with:
          repository: SE-FCaps-Tool/f-caps-schedule-be
          ref: main
          path: .contract-be
          fetch-depth: 1

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm test

      - name: Generate and verify API contract
        run: npm run typegen:check -- .contract-be/apps/api/openapi.json
